# Copy to backend/.env.local (gitignored) — never commit the service role key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
FRONTEND_URL=https://your-production-domain.com
# For local SQLite fallback if not using Supabase yet
# Keep better-sqlite3 for now during migration